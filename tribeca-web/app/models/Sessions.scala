package models

object Sessions {
  var table = new database.Sessions

  def createSessionId{


    /*//String uuid=session("uuid");
    if(uuid==null) {
      val uuid = java.util.UUID.randomUUID().toString();
      //session("uuid", uuid);
    }*/
  }
}
